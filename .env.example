# ── Postgres ──────────────────────────────
POSTGRES_USER=portal
POSTGRES_PASSWORD=portal_dev_pwd
POSTGRES_DB=portal_aluno
POSTGRES_PORT=5432

# ── Redis ─────────────────────────────────
REDIS_PORT=6379

# ── API (.NET 10) ─────────────────────────
ASPNETCORE_ENVIRONMENT=Development
API_PORT=8080
# Troque por um segredo forte (>= 32 chars) em produção
JWT_KEY=dev-only-change-me-portal-aluno-musica-super-secret-key-256bits

# ── Super usuário (professor) — seed inicial ──
ADMIN_NAME=Professor
ADMIN_EMAIL=admin@portal.dev
ADMIN_PASSWORD=troque-esta-senha

# ── Web (React PWA) ───────────────────────
WEB_PORT=3000
VITE_API_URL=http://localhost:8080
